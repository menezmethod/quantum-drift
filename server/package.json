{
  "name": "quantum-drift-server",
  "version": "1.1.0",
  "private": true,
  "description": "Use the root installation for the shared Quantum Drift runtime",
  "scripts": {
    "start": "node server.js",
    "dev": "npm --prefix .. run dev",
    "test": "npm --prefix .. test"
  },
  "engines": {
    "node": ">=20"
  }
}
